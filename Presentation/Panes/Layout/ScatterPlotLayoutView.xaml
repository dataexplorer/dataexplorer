<UserControl x:Class="DataExplorer.Presentation.Panes.Layout.ScatterPlotLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xAxis="clr-namespace:DataExplorer.Presentation.Panes.Layout.XAxis"
             xmlns:yAxis="clr-namespace:DataExplorer.Presentation.Panes.Layout.YAxis"
             xmlns:color="clr-namespace:DataExplorer.Presentation.Panes.Layout.Color"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <xAxis:XAxisLayoutView 
            Grid.Row="0" 
            DataContext="{Binding XAxisLayoutViewModel}" />
        <yAxis:YAxisLayoutView 
            Grid.Row="1" 
            DataContext="{Binding YAxisLayoutViewModel}" />
        <color:ColorLayoutView 
            Grid.Row="2"
            DataContext="{Binding ColorLayoutViewModel}" />
    </Grid>
</UserControl>
