<UserControl x:Class="DataExplorer.Presentation.Views.ScatterPlot.Layout.ScatterPlotLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:DataExplorer.Presentation.Views.ScatterPlot.Layout"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <views:XAxisLayoutView 
            Grid.Row="0" 
            DataContext="{Binding XAxisLayoutViewModel}" />
        <views:YAxisLayoutView 
            Grid.Row="1" 
            DataContext="{Binding YAxisLayoutViewModel}" />
    </Grid>
</UserControl>
